import { APP_TYPES, AppType } from '@scoutarr/shared';

/**
 * Application constants
 */
export const ITEMS_PER_PAGE = 15;
export const MAX_INSTANCES_PER_APP = 4;

/**
 * UI Constants
 */
export const AUTO_RELOAD_DELAY_MS = 1000;

/**
 * Re-export APP_TYPES and AppType from shared package
 */
export { APP_TYPES };
export type { AppType };
